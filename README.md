# Reproduction for Sphinx 4.1 C++ parsing issues

To reproduce, uncomment one of the line in src/index.rst, and run the following
commands in a fresh virtual environment

```
pip install -r requirements.txt
make html
```
